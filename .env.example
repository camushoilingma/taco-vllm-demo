SSH_KEY=~/secrets/your-key.pem
REMOTE_USER=ubuntu
REMOTE_IP=<INSTANCE_IP>

# Tencent COS upload (optional — set to enable auto-upload of benchmark results)
COS_BUCKET=<YOUR_BUCKET_NAME>
COS_PREFIX=logs
